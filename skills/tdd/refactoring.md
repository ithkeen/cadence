# 重构候选

一轮 TDD 之后，看看有没有以下信号：

- **重复代码** → 抽成函数 / 类
- **长方法** → 拆成私有辅助函数（测试仍然只测公共接口）
- **浅模块** → 合并或加深
- **特性依恋（Feature envy）** → 把逻辑挪到数据所在的位置
- **基本类型偏执（Primitive obsession）** → 引入值对象
- **既有代码** → 新代码暴露出它有问题
